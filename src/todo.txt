
personagem
  editar
  escolher ativo
  upar (
    abrir uma nova pagina
    navegar para o lado em grupo de interesses;
  )
  criar(
    pe_cdmagia 8+ mod + bprof
    pe_bonusataquemagia mod + bprof
    pe_habilidadeconjuracao habilidade
  )

principal
  vidas
    salvar inc
    salvar dec
    salvar uso dados
    salvar teste morte

invenario
  trocar modal botao por (import BtnSalvarForm from '../../components/btnsalvarform';)

caracteristica
  trocar modal botao por (import BtnSalvarForm from '../../components/btnsalvarform';)
  
componente vazio
componente erro

tile deslize suave

tirar do auth.js a linha:
  localStorage.setItem('RF@personagemID', 'Ph3YdE5qOua601u0m0Wj'); 

usar hooks a interessantes
  (x) useRef 
  (x) useFormStatus();